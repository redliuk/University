Here we have some interesting exercies to get a grip of how C interacts with a machine:

StarWarsReAlloc
	Focus on functions malloc(), free(), calloc(), realloc(), memcpy().

StringMaster
	Learning the use of <stdio.h> and the difference between array, string char array and pointer in C
	

WHAT I LEARNED
	Compriension on explicit hesp dinamic allocation and differences between array C and vector C++.
	Learnig the first steps on bash.